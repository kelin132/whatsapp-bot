

```js
const ownerNumber = '263719809572@s.whatsapp.net'; // Replace with your number
```

---

```js
sock.ev.on('messages.upsert', async (m) => {
  const msg = m.messages[0];
  if (!msg.message || !msg.message.conversation) return;
  const from = msg.key.remoteJid;
  const text = msg.message.conversation.trim();
```

*Now add this below that line:*

```js
  const sender = msg.key.participant || msg.key.remoteJid;

  if (text === '.admin' && sender === ownerNumber) {
    await sock.sendMessage(from, { text: '👑 You are the bot owner!' });
  } else if (text === '.admin') {
    await sock.sendMessage(from, { text: '❌ You are not allowed to use this command.' });
  }
```

---

